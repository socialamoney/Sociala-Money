# Services

Shared services and API clients.

Domain-specific services should preferably live inside `features/<domain>/services/`.

This folder is reserved for cross-cutting services (email, telegram, etc.).
